

export const metadata = {
    title: "About us - Samlois",
    description: "Real Estate",
  };

const AboutLayout = ({children}) => {
  return (
    <div>
       {children}
    </div>
  )
}

export default AboutLayout